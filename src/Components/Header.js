import React from "react";
import logo from "../img/logo.png";

const Header = () => {
    return (
        <header>
            <img src={logo} />
        </header>
    );
}

export default Header;